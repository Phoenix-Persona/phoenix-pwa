# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*


node_modules
dist
dist-ssr
*.local
.eslintcache
.tsbuildinfo
test-results/
.ai/
yarn.lock

# Editor directories and files
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Vercel
.vercel

# Secrets
.env
.env.*
!.env.example

# Local ppq.ai test credentials (grants spending power — never commit)
test/manual/ai-services/.account.json
test/manual/ai-services/.last-invoice.json

# Veo last-frame-conditioning test artifacts (clip MP4s + extracted frame)
test/manual/ai-services/.veo-last-frame/

# i2v chain test scratch dirs (per-run mp4s + extracted frames)
test/manual/ai-services/.i2v-chain/
test/manual/ai-services/.i2v-chain-english-paced/

# Parallel-runner per-run logs
test/manual/ai-services/.monologue-runs/

# Local wallet test credentials (grants spending power — never commit)
test/manual/wallet/.operator.json
test/manual/wallet/.persona.json

# Breez Spark SDK persistent storage (created by Node tests at storageDir
# = "phoenix-wallet"; relative to wherever the script ran from)
phoenix-wallet/

SCRATCH.md

# Capacitor Android signing — never commit keystore passwords or .jks files
android/keystore.properties
android/*.jks
android/*.keystore

.tmp/
.claude/
.vscode/
SCRATCH.md